{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE \"(rm -rf /|DROP TABLE|git push --force)\"; then echo \"[one-way-door warning] This command is hard to undo. Read it twice before continuing.\" >&2; fi; exit 0'"
          }
        ]
      }
    ]
  }
}
